model_name: "fraud-detector"
model_stage: "Production"

decision_threshold: 0.5  # Probability above which we block the transaction
max_latency_ms: 100

api:
  host: "0.0.0.0"
  port: 8000
  reload: false

predictions_log: "data/predictions/recent.csv"
